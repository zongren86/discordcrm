# ==========================================
# Local 开发环境 — 必须连本地 MySQL
# 激活: --spring.profiles.active=local
# ==========================================

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/discordadmin?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8&createDatabaseIfNotExist=true
    username: root
    password: Len2026!

  jpa:
    show-sql: true

logging:
  level:
    root: INFO
    com.discordadmin: DEBUG
